import React from 'react';
import './styles.scss';
import { UiButtonProps } from './types';
export declare const UiButton: React.MemoExoticComponent<({ className, buttonType, disabled, testId, children, ...buttonProps }: UiButtonProps) => JSX.Element>;
export { ButtonTypes } from './types';
